

export const loginUser = (username, password) => async dispatch => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}

export const logoutUser = () => async dispatch => {
    try {

    } catch (error) {
        console.log(error);
    }
}